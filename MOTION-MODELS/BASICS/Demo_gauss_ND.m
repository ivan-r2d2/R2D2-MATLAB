clc; clear all; close all;

%----------------------------------------------------------------------%
% 1. TAKE SAMPLES
%----------------------------------------------------------------------%
MU = [2 3];
SIGMA = [1 1.5; 1.5 3];
X = mvnrnd(MU,SIGMA,10);

%----------------------------------------------------------------------%
% 2. COMPUTE PDFS
%----------------------------------------------------------------------%
p1 = mvnpdf(X,MU,SIGMA);
p2 = gauss_ND(X,MU,SIGMA); 
error = p1 - p2;
disp('Error maximo')
disp(max(error))
